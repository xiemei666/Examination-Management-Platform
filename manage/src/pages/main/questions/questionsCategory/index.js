import React from 'react';
import { connect } from 'dva';


function questionsCategory() {
    return (
        <div>
            2222
        </div>
    );
}

questionsCategory.propTypes = {
};

export default connect()(questionsCategory);
